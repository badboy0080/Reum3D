# 动画简历制作器

用户在浏览器里创建「项目」，使用固定 3D 形象 Chao，按「页面」编辑镜头与文本，并导出可播放的动画简历。

## Language

**项目**：
用户创建的一份可编辑、可导出的动画简历工作稿。
_Avoid_：工程、仓库、文档（泛指）

**形象**：
出现在 3D 场景中的角色外观，当前固定为 **Chao**（本地 **GLB** 模型）；作为镜头运镜的锚点。播放全程形象都在场，页与页之间变换的是文字（及机位），不是换人。导出时：大模型改为压缩包（html + 形象文件）。
_Avoid_：Avatar（口语可提，文档用「形象」）、多套可选角色、捏脸角色、网页端直接使用 FBX、仅首页有形象、程序化胶囊占位（已退役）

**标签**：
页面在导航上的短名（如 OVERVIEW、WORKS），用于「点标签、镜头飞过去」的交互，接近 intro3d 的 stop tag。
_Avoid_：把标签当成正文内容本身

**页面**：
简历中的一屏内容单元。默认含首页、简介页、作品页、联系页（可改标题与内容，不可删除；可隐藏后不参与播放）；用户最多再新增 4 个自定义页面（合计不超过约 8 页）。
_Avoid_：区块、Section（不以 Section 作为主结构）、幻灯片、不限页数、删除默认四页

**机位**：
某一页绑定的相机空间坐标与角度（相对形象锚点的推拉摇移结果）。每页一个机位。
_Avoid_：镜头关键帧时间轴（不以自由时间轴为主）、自由运镜路径

**运镜预设**：
编辑机位时可选的成套视角方案（如正面半身、侧面、俯视、特写），并可再微调 X（左右）、距离与高度；预览支持中键平移与滚轮推拉。
_Avoid_：纯手填坐标作为唯一方式

**角色旋转中心**：
左键轨道旋转所绕的固定点，取角色身体中心（约胸口高度），不随中键平移而移动；对应 Blender 里「绕物体转」的 orbit center，而不是画面中心。
_Avoid_：画面中心、脚底模型原点（除非明确改回）、随中键平移一起被带走的看向点

**转盘旋转（Turntable）**：
左键绕角色旋转中心时：左右绕竖直轴转、上下抬头低头；保持头朝上，不使用会把角色拧歪的轨迹球（Trackball）。
_Avoid_：Trackball 轨迹球旋转

**摄影机平移**：
中键拖动只改变摄影机位姿（画面可整体平移，角色可以离开画面正中）；不得改写角色旋转中心。松手后再左键旋转，仍绕角色旋转中心，而不是绕当前画面正中。
_Avoid_：把平移做成挪动角色、把平移中心当成新的旋转中心、Blender 默认那种「平移后旋转中心跟着视口走」

**出血线**：
固定叠在舞台画面上的裁切/构图参考线，距外边框 40px；用户可选样式（金角准星、银角细线、全框细线等），始终显示。
_Avoid_：可拖动出血线、每页不同出血距、打印 PDF 真实出血（本期不做）

**场景背景 / 背景色板**：
每页各自的 3D 舞台背景预设（存在 `Page.backgroundPresetId`）。默认提供多套色板（纯色或自上而下深色渐变：可可 / 墨蓝 / 松烟 / 暮紫），并联动雾效与主光气质；翻页时背景可随之切换。
_Avoid_：每页各一套复杂 HDR、任意自定义色值面板（第一期不做）、仅改 CSS 不改 3D 清屏色

**页间运镜**：
用户切换到下一页/上一页时，相机从当前页机位缓动到目标页机位。
_Avoid_：硬切（除非用户明确选择）、与页面无关的全局时间轴 scrub（可作为预览辅助，不是主交互）

**文本块**：
挂在某一页上的可编辑文字单元；可在预览中拖拽调整位置；侧栏可调字号、字体、字重、颜色、对齐与入场/出场。
_Avoid_：纯表单字段（编辑器里可以是表单，领域上仍是「文本块」）、任意自定义字体上传（本期不做）

**图片卡片组**：
挂在某一页上的多图展示模块（Amicro A1–A12 布局）。每组可选 12 种布局之一，卡片数量 2–12 张，每张含图片与标题；支持粘贴 URL 或本地上传。悬停类布局在鼠标移入时扇开/散开；轮播/CoverFlow/时间机器通过点击切换。整组可拖拽位置，并配置简单入场/出场（淡入、缩放 / 淡出）。
_Avoid_：每张卡单独配打字机等文本入场、远端图床、无限上传大图

**入场方式**：
文本块出现时的动画类型。第一期目录：`fade-in`、`slide-up`、`slide-left`、`zoom-in`、`typewriter`、`stagger-lines`。
_Avoid_：开放式参数面板、把整个第三方组件库原样暴露给用户

**出场方式**：
文本块离开或翻页清场时的动画类型。第一期目录：`fade-out`、`slide-down`、`none`。
_Avoid_：与入场无关的任意效果堆砌

**动画组件库**：
用于编辑器微交互（Amicro 图片卡片布局，Motion）与页间/文本入场（GSAP）。
_Avoid_：AI 能力、大模型写简历、智能排版（本期不做）

**导出稿**：
用户导出的可独立播放的动画简历成品。第一期优先单个交互 HTML；体积过大时可降级为压缩包（html + 形象资源）。
_Avoid_：预览页、编辑器本身、第一期强制出视频

**成片视频**（二期候选）：
将页间运镜与文字动画录成固定视频文件（如 MP4）。技术选型在 Remotion 与 Hyperframes 之间评估后再定，不作为第一期交付。
_Avoid_：第一期唯一交付物、未评估就同时上两套渲染管线
