# 以页面为中心的简历结构与页间运镜

用户要的是「翻页式」动画简历：每一屏绑定一个机位，点下一页时相机相对全程在场的形象做缓动推拉摇移，同时切换该页文本块的入场/出场。若采用自由镜头关键帧时间轴 + Section 区块，主交互会偏离翻页心智，编辑与导出都会更难验收。

因此第一期以 **页面（Page）** 为唯一主结构：每页一机位、形象不换人只换文与机位；导出优先交互 HTML。成片视频（Remotion vs Hyperframes）留待二期评估。
